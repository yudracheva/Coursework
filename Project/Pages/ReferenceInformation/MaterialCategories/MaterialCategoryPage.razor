@page "/material-categories/{id:int}"
@page "/material-categories/new"
@inherits MaterialCategoriesPageIndex

@if (isLoad)
{
    @if (materialСategory != null)
    {
        <h3>Категория материалов @GetDescription()</h3>

        <form>
            @if (materialСategory.Id != 0)
            {
                <div class="form-group row">
                    <label class="col-sm-2 col-form-label">Идентификатор: </label>
                    <div class="col-sm-10">
                        <input type="text" readonly class="form-control-plaintext" value="@materialСategory.Id">
                    </div>
                </div>
            }

            <div class="form-group row">
                <label class="col-sm-2 col-form-label">Наименование: </label>
                <div class="col-sm-10">
                    <input type="text" class="form-control form-control-sm" @bind="@materialСategory.Name" />
                </div>
            </div>

            <button type="button" class="btn btn-info" @onclick="Save">Сохранить</button>
        </form>
    }
}
else
{
    <LoadComponent />
}