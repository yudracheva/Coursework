@page "/suppliers"
@inherits SuppliersPageIndex

<h3>Поставщики</h3>

@if (isLoad)
{
    <div class="row mb-2">
        <div class="col">
            @{
                var adressToCreate = "/suppliers/new";
            }
            <button class="btn btn-sm btn-outline-info" @onclick="(()=>NavigationManager.NavigateTo(adressToCreate))">Создать</button>
        </div>
    </div>

    <table class="table table-striped table-hover table-sm">
        <thead>
            <tr>
                <th>
                    Наименование
                </th>
                <th>
                    ИНН
                </th>
                <th>
                    КПП
                </th>
                <th>
                    Email
                </th>
                <th>

                </th>
            </tr>
        </thead>

        @if (suppliers != null)
        {
            <tbody>
                @foreach (var supplier in suppliers)
                {
                    <tr>
                        <td>
                            <a href="/suppliers/@supplier.Id.ToString("0")">@supplier.OrganizationName</a>
                        </td>
                        <td>
                            @supplier.INN
                        </td>
                        <td>
                            @supplier.KPP
                        </td>
                        <td>
                            @supplier.Email
                        </td>
                        <td>
                            <div class="row float-right">
                                <div class="col-1 mr-1"><button class="btn btn-sm btn-outline-secondary" title="Редактировать" @onclick="(()=> Edit(supplier.Id))"><span class="oi oi-pencil"></span></button></div>
                                <div class="col-1"><button class="btn btn-sm btn-outline-secondary" title="Удалить" @onclick="(()=> Remove(supplier.Id))"><span class="oi oi-x"></span></button></div>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        }
    </table>

}
else
{
    <LoadComponent />
}
